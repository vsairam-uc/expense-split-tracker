# Copy to .env and fill in real values (never commit .env)
# Local DB defaults match docker-compose.yml when using npm run db:up

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/split_expense_tracker?schema=public"

# Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-random-secret"

AUTH_URL="http://localhost:3000"
